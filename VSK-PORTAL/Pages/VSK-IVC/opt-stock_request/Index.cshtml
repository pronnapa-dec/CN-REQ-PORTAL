@page "/ivc/opt/stock_request"

@{
    ViewData["Script"] = "apps/ivc/opt-stock_request.js";

    ViewData["Title"] = "Index";
    ViewData["Content-Title"] = "Stock Request";
    ViewData["Content-Title-Page"] = "รายการคำขอสต็อก";

}

@* container opened *@
<div class="container">

    @* subheader *@
    <div class="breadcrumb-header justify-content-between">

        @* breadcrumb *@
        <div class="my-auto"> 
            <div class="d-flex">
                <h4 class="content-title mb-0 my-auto">@ViewData["Content-Title"]</h4><span class="text-muted mt-1 tx-13 ml-2 mb-0">/ @ViewData["Content-Title-Page"]</span>
            </div>
        </div>
        @* breadcrumb *@
        <div class="d-flex my-xl-auto right-content">
            @*<div class="mb-6 mb-xl-0 mg-r-10">
                    <button id="btn-report_loscan" class="btn btn-dark btn-with-icon btn-block"><i class="las la-barcode"></i> Report Loscan </button>
                </div>*@
            <div class="mb-6 mb-xl-0 mg-r-10">
                <button id="btn-report_stock_request" class="btn btn-dark btn-with-icon btn-block"><i class="las la-file-excel"></i> รายงานคำขอสต็อก</button>
            </div>
            <div class="mb-3 mb-xl-0">
                <button id="btn-item_create" class="btn btn-primary btn-with-icon btn-action btn-block"><i class="typcn typcn-document-add"></i>สร้าง</button>
            </div>
        </div>
    </div>
    @* subheader *@

    @* modal form *@
    <div id="card-form" class="row">
        <partial name="~/Pages/VSK-IVC/opt-stock_request/Form.cshtml" />
    </div>
    @* modal form *@

    @* datatables *@
    <div id="card-datatables" class="row">
        <partial name="~/Pages/VSK-IVC/opt-stock_request/List.cshtml" />
    </div>
    @* datatables *@

    @* modal form *@
    <div id="card-form" class="row">
        <partial name="~/Pages/VSK-IVC/opt-stock_request/History.cshtml" />
    </div>
    @* modal form *@   
    
    @* modal form *@
    <div id="card-form" class="row">
        <partial name="~/Pages/VSK-IVC/opt-stock_request/Approval.cshtml" />
    </div>
    @* modal form *@

</div>
@* Container closed *@
